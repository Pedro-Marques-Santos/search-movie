"use client";

import { styled } from "styled-components";

export const Container = styled.div`
  width: 100%;
  max-width: 360px;
  background: var(--black-800);
  box-shadow: var(--black-900) 0px 7px 29px 0px;
  border-radius: 8px;
  margin-left: 8px;
  margin-right: 8px;
`;
